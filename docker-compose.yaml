
services:
  osdev:
    build: .
    container_name: osdev-environment
    volumes:
      # Mount the entire bootloader directory
      - .:/workspace
      # Mount home directory for persistent shell history
      # - ./home:/home/osdev
    stdin_open: true
    tty: true
    # Keep container running
    command: tail -f /dev/null
 